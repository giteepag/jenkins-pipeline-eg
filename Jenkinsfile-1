pipeline {
  agent {
    docker { 
    image 'aws-pip1' 
    args '-u root -v ~/.aws:/root/.aws:ro --network="host"'
    }
  }
  stages {
    stage('Build') {
      steps {
        sh 'aws --version'
      }
    }
  }
}
