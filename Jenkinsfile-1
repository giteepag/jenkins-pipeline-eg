pipeline {
  agent {
    docker { 
    image 'aws-pip1' 
    args '-u root -v /home/prashant/.aws:/root/.aws:ro --network="host"'
    }
  }
  stages {
    stage('Build') {
      steps {
        sh 'aws --version'
      }
    }
  }
}
